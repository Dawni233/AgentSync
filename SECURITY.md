# 安全策略

## 报告漏洞

如果你发现 AgentSync 的安全漏洞，请**私下**上报，不要在公开 Issue 中讨论。

上报方式：发送邮件至 `Dawni233@users.noreply.github.com`，主题以 `[SECURITY] AgentSync` 开头。

请在邮件中说明：

- 漏洞类型与受影响版本
- 复现步骤（最小化用例）
- 影响范围评估
- 建议的修复方案（如有）

收到报告后，我会在 72 小时内确认收悉，并在评估后给出修复时间表。修复并发布新版本后，会在 [CHANGELOG.md](./CHANGELOG.md) 中致谢上报者（除非你要求匿名）。

## 已知安全限制

AgentSync 当前处于 MVP 阶段，以下限制已在 [README.md](./README.md) 中公开披露。使用前请充分了解：

### PAT 明文存储

Git PAT（Personal Access Token）目前**明文存储**在本地 SQLite 数据库中，尚未迁移到系统 keychain。

- 影响范围：任何能读取应用数据目录（`%APPDATA%/com.dawni.agentsync` 等）的本机进程都能获取 PAT
- 缓解建议：使用**最小权限**的 PAT（仅赋予单个私有仓库的读写权限），不与不可信账户共享设备
- 后续计划：迁移到操作系统级凭据存储（Windows Credential Manager / macOS Keychain / Linux Secret Service）

### CSP 未启用

Tauri 的内容安全策略（CSP）当前配置为 `null`，未启用。

- 影响范围：若未来引入外部资源加载，缺少 CSP 防护会增加 XSS 攻击面
- 缓解建议：当前应用仅加载本地打包资源，不加载任何远程内容
- 后续计划：启用严格 CSP

### 人格包导入风险

导入 `.zip` 人格包时存在两类已知风险：

1. **Zip slip**：导入时未对 zip 内路径做严格的穿越校验，恶意人格包可能将文件写入目标目录之外的位置
2. **Prompt 注入**：人格文件本质是 AI 系统提示词，恶意人格包可能包含 prompt 注入内容，影响 Agent 行为

- 缓解建议：**仅导入可信来源的人格包**，导入前务必在 diff 预览界面审查文件内容与路径
- 后续计划：增加 zip 路径穿越校验

### 路径穿越防护

文件预览接口已校验 `..` 路径组件，防止目录穿越读取应用数据目录之外的文件。

## 使用建议

- **务必使用私有 Git 仓库**作为同步后端。Agent 配置文件可能包含敏感信息（系统提示词、个人偏好等），公开仓库会导致泄露
- 使用最小权限 PAT，定期轮换
- 不在不可信设备上配置同步
