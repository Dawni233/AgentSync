\## 编码前思考

\- 明确假设，不确定时询问而非猜测。

\- 存在歧义时，列出多种解释，不默默选定单一方案。

\- 如果任务有明显更简单的做法，直接指出优化思路。

\- 发现代码矛盾、逻辑不一致时及时暂停，请求信息澄清。



\## 简洁优先

\- 用最少的代码解决问题，拒绝冗余实现。

\- 不为一次性需求创建抽象层、复杂架构。

\- 不盲目增加扩展性、可配置性，应对“未来可能用到”的场景。

\- 若代码可大幅精简，主动重写优化。

\- 校验标准：以资深工程师视角判断，代码若过于复杂，立即简化。



\## 精准修改

\- 仅修改与当前任务直接相关的代码内容。

\- 不顺手优化相邻代码、注释、排版格式。

\- 不重构原本可以正常运行的代码模块。

\- 严格匹配项目现有代码风格，保留原有编码习惯。

\- 因本次修改产生的无效导入、废弃变量，可直接删除。

\- 发现项目中原有的死代码、冗余内容，仅做文字提醒，不擅自删除。



\## 目标驱动执行

\- 执行任务前，定义清晰、可落地的成功标准。

\- 将“修复Bug”转化为：编写用例复现问题，再调试至用例正常通过。

\- 将“新增校验功能”转化为：针对异常输入编写测试用例，保证全部通过。

\- 将“代码重构”转化为：完成重构后，确保原有所有测试用例正常运行。

\- 多步骤复杂任务，先输出简短执行计划，同时标注每一步的验证方式。



\## 设计文档对照

\- 项目存在规格/设计文档（如 `design.md`、`PRD`、接口文档）时，文档条款优先级高于个人经验与惯例。

\- 编码前必查文档：动手实现任意模块/组件前，重读文档对应章节，把其中的明确规格（字段、布局、状态、参数、命名等）列为该模块的验收标准。

\- 完成验证必含「设计符合度」检查：任务完成时的验证清单不能只有「能编译 / 能跑 / 测试通过」等技术项，必须额外包含「实现是否符合文档对应章节」的逐条对照。漏检即视为未完成。

\- 区分「功能实现」与「结构骨架」：Phase 归类只决定功能（IPC、数据流、业务逻辑）何时落地，不推迟布局/结构骨架。HTML 结构、目录划分、模块占位应在最早涉及该模块的 Phase 按文档搭好，内容用占位 -- 不允许以「功能未实现」为由跳过结构。

\- 个人偏好不覆盖文档条款：当个人判断（如「暗色更适合开发者工具」「这个字段没必要」）与文档明确条款冲突时，按文档执行；若确需偏离，先向用户提出并说明理由，不擅自改掉。

\- 评审与执行使用同一份文档：评审设计文档时的细致程度，必须延续到执行阶段。不允许「评审时很熟、编码时凭印象」-- 编码每个组件前都要回头查文档，而不是依赖记忆。



\## 版本与发布

\- 版本号遵循语义化版本（SemVer）：`MAJOR.MINOR.PATCH`，如 `0.1.0`。

\- 版本号唯一来源：`src-tauri/tauri.conf.json` 的 `version` 字段。`package.json` 的 `version` 保持同步。

\- 不兼容的 API/数据格式变更必须升 MAJOR；新增功能升 MINOR；Bug 修复升 PATCH。

\- 每次发布前必须更新 `tauri.conf.json` 的 `version`，并在 git 提交信息中包含版本号（如 `release: v0.2.0`）。

\- 发版产物命名格式：`FileWise-v{版本号}-{平台}-{架构}.{扩展名}`。

\- 示例：
  - `FileWise-v0.1.0-windows-x64.zip`（绿色版）
  - `FileWise-v0.1.0-windows-x64.msi`（MSI 安装包）
  - `FileWise-v0.1.0-windows-x64-setup.exe`（NSIS 安装器）
  - `FileWise-v0.1.0-macos-universal.dmg`（macOS）
  - `FileWise-v0.1.0-linux-amd64.deb`（Linux）

\- 平台标识使用小写：`windows` / `macos` / `linux`；架构标识：`x64` / `arm64` / `universal`。

\- 绿色版 zip 必须包含 `FileWise/` 根目录，所有文件（exe + dll + models/）放在该目录下，附 `使用说明.txt`。

\- 不打包无用文件：仅包含运行所需的主程序、动态库、模型文件、分词器；FP32 原模型等不打入发行包。

\- 发布前自检：将产物复制到陌生目录（脱离源码环境）启动验证，确保依赖完整、模型加载成功、核心功能可用。

\- 发布渠道优先级：GitHub Releases > 网盘 > 邮件附件。每版本附 changelog（本版本相对上一版的变更点）。

